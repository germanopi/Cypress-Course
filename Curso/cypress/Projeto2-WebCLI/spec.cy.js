describe('template spec', () => {
  // Agrupa o(s) teste(s) sob o título 'template spec'

  it('passes', () => {
    // Define um teste individual chamado 'passes'
    
    cy.visit('https://example.cypress.io') 
    // Acessa a URL 'https://example.cypress.io' no navegador de teste
    // Esse comando é usado para verificar se a página carrega corretamente
  })
})